<!DOCTYPE html>
<html>
    <head>
        <title>Welcome to OpenDXP!</title>

        <meta charset="UTF-8">
        <meta name="robots" content="noindex, follow">

        <link rel="icon" type="image/png" href="/bundles/opendxpadmin/img/favicon/favicon-32x32.png">

        <link rel="stylesheet" href="/bundles/opendxpadmin/css/login.css" type="text/css">

        {% for pluginCssPath in pluginCssPaths %}
            <link rel="stylesheet" type="text/css" href="{{ pluginCssPath }}?_dc={{ 'now'|date('U') }}">
        {% endfor %}
    </head>
    <body class="opendxp_version_1 {{ adminSettings['branding']['login_screen_invert_colors'] ? 'inverted' : '' }}">
        <style>
            #background {
                background-image: url("{{ opendxp_login_background_image('/bundles/opendxpadmin/img/login/login-background.png') }}");
            }
        </style>

        {% set customColor = adminSettings['branding']['color_login_screen'] %}
        {% if (customColor is not empty) %}
        <style>
            #content button {
                background: {{ customColor }};
            }

            #content a {
                color: {{ customColor }};
            }
        </style>
        {% endif %}

        <div id="logo">
            <img alt="{{ "OpenDXP logotype"|trans([], 'admin') }}" src="{{ path('opendxp_settings_display_custom_logo') ~ (adminSettings['branding']['login_screen_invert_colors'] ? '' : '?white=true') }}">
        </div>

        <div id="content">
            {{ block('content') }}
        </div>

        {% if debug is defined and debug %}
            <div id="github">
                <a class="github-button" href="https://github.com/open-dxp/opendxp" data-color-scheme="no-preference: dark; light: dark; dark: dark;" data-size="large" aria-label="Star open-dxp/opendxp on GitHub">Star</a>
            </div>
            <script async defer src="https://buttons.github.io/buttons.js" {{ opendxp_csp.getNonceHtmlAttribute()|raw }}></script>
        {% endif %}

        <div id="contentBackground"></div>
        <div id="background"></div>
        <div id="footer">
            &copy; {{ 'now'|date('Y') }} <a href="http://www.opendxp.ch/">OpenDXP</a><br>
            BE RESPECTFUL AND HONOR OUR WORK FOR FREE & OPEN SOURCE SOFTWARE BY NOT REMOVING OUR COPYRIGHT NOTICE!
            KEEP IN MIND THAT REMOVING THE COPYRIGHT NOTICE IS VIOLATING OUR LICENSING TERMS!
        </div>

        {% if block('below_footer') is defined %}
            {{ block('below_footer') }}
        {% endif %}

    </body>
</html>
